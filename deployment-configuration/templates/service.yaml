apiVersion: v1
kind: Service
metadata:
  name: {{ include "deployment-configuration.fullname" . }}
  labels:
    {{- include "deployment-configuration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deployment-configuration.selectorLabels" . | nindent 4 }}
